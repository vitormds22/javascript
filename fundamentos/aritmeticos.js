const [a, b, c, d] = [3, 5, 1, 15]

//Sempre binária, com dois elementos
const soma = a + b + c + d

// ++b //prefix
// b++ // sufix
// b + a //infix

const subtracao = d - b
const multiplicacao = a * b 
const divisao = a / b 
const modulo = a % b 

console.log(soma, subtracao, multiplicacao, divisao, modulo)